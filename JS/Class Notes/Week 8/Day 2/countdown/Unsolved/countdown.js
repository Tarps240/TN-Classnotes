// Write code to print all numbers from `num` down to 1
// You may assume `num` will be a positive number

// function countdown(num) {
//     for (let i = num; i >= 1; i--) {
//       console.log(i);
//     }
//   }
  
//  countdown(100); // Print numbers from 50 down to 1


 // A function to iterate numbers from 1 up to ten.
 // A for loop that declares a starting number and counts up to the desired number.

 function countup (num) {
  for (let i = 1; i <= num; i++) {
    console.log(i);
  }
 }
 countup(50);